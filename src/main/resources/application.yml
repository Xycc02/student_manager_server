spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/studentms?setUnicode=true&characterEncoding=utf8&useAffectedRows=true
    username: root
    password: root

mybatis:
  type-aliases-package: cn.shu.student_server.entity
  mapper-locations: classpath:mapper/*.xml

server:
  port: 10086